# Calcite → Alembic port audit

Alembic ports the core of Apache Calcite's planner — the cost-based (Volcano/Cascades) and heuristic
(HEP) optimizers, made medium-agnostic. The **per-member Calcite → Alembic mapping lives in code**, as
`[Provenance(className, member?)]` attributes on every ported type and member; that is the authoritative,
machine-readable ledger. This file keeps only what *can't* live in an attribute: what was deliberately
**not** ported (§1), the **fidelity re-audit** of divergences from Calcite (§2), and tracked **follow-ups**
(§3).

**Naming.** "Op" is Alembic's counterpart to Calcite's "Rel" — the medium-agnostic operation the engine
plans over. So `RelNode` → `IOp`, `PhysicalNode` → `IPhysicalOp`, `SingleRel` → `SingleOp`,
`RelOptCluster` → `OpCluster`, `RelTraitSet` → `OpTraitSet`, etc.; the optimizer "Opt" and the tree-node
"Node" are both dropped. Members keep their Calcite-faithful names (`getCluster()` → the `Cluster`
property, and so on).

---

## 1. Deliberately not ported

Whole subsystems Alembic does not implement, by design or as future work. **Scope rule:** the *only*
legitimately out-of-scope category is genuinely **relational** (row types, Rex, SQL, materialized views,
hints, correlation, the operator library). Everything else (metadata, debug, cancellation, timeouts) is
deferred/unported work, not "out of scope", and is fair game for the audit.

| Calcite | Status |
|---|---|
| Relational metadata kinds (`RowCount`, `Selectivity`, `DistinctRowCount`, `ColumnUniqueness`, `UniqueKeys`, `ColumnOrigin`, `ExpressionLineage`, `TableReferences`, `Predicates`/`AllPredicates`, `Collation`, `Distribution`, `Size`, `PopulationSize`, `PercentageOriginalRows`, `Max`/`MinRowCount`, `ExplainVisibility` (SqlExplainLevel), `InputFieldsUsed` (field bitset), `Measure` (Rex), `FunctionalDependency` (columns), …) and their `RelMd*` providers | **not ported** (relational — rows/columns/predicates/tables/SQL) |
| `NodeTypes` (structural — `Multimap<op-class, op>`) | **non-relational, portable, currently unported** — no planner consumer; port on request |
| `RelMetadataProvider` / `ReflectiveRelMetadataProvider` / `ChainedRelMetadataProvider` / `DefaultRelMetadataProvider` / `ProxyingMetadataHandlerProvider` (reflective dispatch) | **ported** as `IOpMetadataProvider` / `ReflectiveOpMetadataProvider` / `ChainedOpMetadataProvider` / `DefaultOpMetadataProvider` / `ProxyingMetadataHandlerProvider`, using **.NET reflection** (`MethodInfo` + `DispatchProxy` for Java's `Method` + `Proxy`). `DefaultOpMetadataProvider` is the explicit handler list (as Calcite's); `ProxyingMetadataHandlerProvider` reflects each handler's typed methods (`CouldImplement`) and routes by the op's runtime type (`Space.find`) to the most-specific one. Md classes (`OpMdLowerBoundCost`, `OpMdMemory`, `OpMdParallelism`, `OpMdCumulativeCost`, `OpMdNonCumulativeCost`) are typed-method bags with a `SOURCE`. |
| `JaninoRelMetadataProvider` (runtime bytecode-generated dispatch) | **not ported** — the reflective `ProxyingMetadataHandlerProvider` path is used instead (Calcite's own non-Janino path). The deprecated inner `Metadata`-proxy layer (`UnboundMetadata`, the `Metadata` accessor methods) is also skipped: it needs a `Memory()` method inside `interface Memory`, which C# forbids (member = enclosing type). |
| Metadata kinds | **non-relational kinds ported**: cost (`CumulativeCost`/`NonCumulativeCost`/`LowerBoundCost` — D12/D22), `Memory`, `Parallelism`, on `OpMetadataQuery` with the per-query cache + `DelegatingMetadataRel`. |
| `RelBuilder` and the relational operator library | **out of scope** — relational |
| `RexNode`, `RelDataType`/row types, row counts, hints, correlation, schemas | **out of scope** — relational |
| `RelShuttle`/`RexShuttle` | **out of scope** — shuttles (relational) |
| `RelVisitor` | **ported** — `OpVisitor` (`Go`/`Visit`/`ReplaceRoot`, descending via `IOp.ChildrenAccept`) |
| `RelNode.getId()` global id sequence | **ported** — `IOp.Id` / `AbstractOp.Id`, atomic creation-order counter (`NEXT_ID.getAndIncrement()`) |
| `Dumpers`, `assertNoCycles`, `assertGraphConsistent`, `dumpGraph`, `VolcanoTimeoutException` | **debug/ops, not ported** (not relational — see audit) |
| materialized views (`RelOptMaterialization`) | **out of scope** — relational |
| `Convention.getRelFactories` | **out of scope** — relational operator factories |
| `RelRule.Config` / per-rule `Config` (immutables) | **n/a** — Alembic ships no rule library; rules take ctor args |

---

## 2. Fidelity re-audit (2026-06-24, fresh cold-fork agents vs local Calcite)

A from-scratch pass against the local Calcite checkout at **`D:\calcite`, ref `calcite-1.41.0-272-g0db20c0b1`**.
One isolated cold-fork `general-purpose` agent per Calcite class (~45 agents over five waves), fed only the
.NET file path + the Calcite file path + the `[Provenance]` mapping, asked whether each ported member is the
*same implementation* as its Calcite original. The agents are **cold**: they do not know the prior audit's
owner decisions, so this pass reports the **raw divergences** the code presents today. Several historical
`D<n>` resolutions therefore reappear below as the cold agent re-derives them — those are kept as findings
**to re-confirm against the recorded decision**, not as regressions.

**Scope:** only genuinely **relational** concerns are auto-out-of-scope (row types, Rex, SQL, materialized
views, hints, correlation, the operator library, relational metadata). Dropped Java `assert`s/`checkArgument`s
(faithful form = `Debug.Assert`), `checkCancel`, listener events, metadata-query plumbing, timeouts,
collection-ordering choices, and structural rewrites are **genuine divergences flagged for a decision**, not
pre-approved.

Legend: 🔴 behaviour-affecting / defect to fix · ⚠️ divergence needing a decision (behaviour-preserving,
unverified, or deferred-feature) · 🔵 blocked on an unported subsystem / minor structural · ✅ verified
faithful. Refer to findings by `A<n>`.

**Roll-up (this pass):** 2a 🔴s triaged via cold-fork re-port agents (2026-06-24) — **A3/A4/A5 resolved**, **A1/A2 re-scoped to display-only** (the audit over-flagged them). ~55 ⚠️ remain (the large majority are dropped `Debug.Assert`-class guards and ordering/collection-type choices) · the rest 🔵/minor.

### 2a. Highest-signal — triaged 2026-06-24 (cold-fork re-port agents)

| # | Alembic member | Calcite member | Finding |
|---|---|---|---|
| ⚠️ A1 | `OpSubset` (no String-digest override) | `RelSubset.getDigest()` | **RE-SCOPED — display-only, not identity.** The audit conflated Calcite's two digests: `getRelDigest()` (the `RelDigest` *object*, the planner's dedup key, `final`) vs `getDigest()` (a *String*, fed only to `toString()`). `RelSubset.getDigest()` overrides the **String** form. Alembic's `GetDigest()` is the port of `getRelDigest()` (the object); the planners dedup on it via `DeepEquals`/`DeepHashCode`, and `OpSubset`'s set-id participates through its `subset` explain term — so identity is unaffected. The String `getDigest()` / `AbstractRelNode.toString()` path was never ported (no String digest member, no op `ToString`). Porting the cosmetic `OpSubset#<set>.​<traits>` string would require **adding that public String-digest + `ToString` mechanism wholesale** — an owner decision, deferred. No behavioural defect. |
| ⚠️ A2 | `HepOpVertex` (no String-digest override) | `HepRelVertex.getDigest()` | **RE-SCOPED — display-only, not identity** (same two-digest conflation as A1). `HepOpVertex` already overrides `DeepEquals`/`DeepHashCode` on `_currentOp.Id` (D4), which is what the HEP map keys on; `HepRelVertex.getDigest()` is the String/`toString` form, unported. Cosmetic `HepOpVertex(<currentOp>)` string is the same deferred owner decision as A1. No behavioural defect. |
| ✅ A3 | `HepPlanner.ApplyRule` fired-rules cache | `HepPlanner.applyRule` | **RESOLVED (cold re-port + owner review).** The cold agent's first cut only re-typed the bespoke `FiredKey` wrapper (id-list instead of op-list) — but Calcite has no such wrapper: it keys `Multimap<ImmutableIntList, RelOptRule>` directly by `ImmutableIntList`, with a `Multimap<Integer, ImmutableIntList>` reverse index. On review this was redone faithfully: ported `Util.ImmutableIntList` (Calcite's real key type, value-equal as a `List<Integer>`), added `Multimap.RemoveAll(key)`, rebuilt both caches as `Multimap<ImmutableIntList, OpRule>` / `Multimap<int, ImmutableIntList>`, and **deleted `FiredKey`**. `ApplyRule` and `RemoveFiredRules` now mirror `applyRule` (656–688) and the GC `removeAll` paths (1164/1233–1236) line-for-line. |
| ✅ A4 | `VolcanoRuleCall.MatchRecurse` | `VolcanoRuleCall.matchRecurse` | **RESOLVED (cold re-port).** Restored all four `MatchedClass == typeof(OpSubset)` branches and the "OpSubset should not match" guard (Calcite `matchRecurse` 292–380): ascending asserts + guard; the unordered branch flattens `GetSubsetsSatisfyingThis()` (no dedup, as Calcite's `flatMap`); the ordered branch uses `GetSubsetsSatisfyingThis()` vs `GetRels()`; the ascending child-check uses `noneMatch(previous::equals)`. Java asserts → `Debug.Assert`. |
| ✅ A5 | `OpRule` operand factories + `OpRuleOperand` ctor + `OpRuleOperandChildren` | `RelOptRule.operand`/`operandJ`/`some`/`none`/`any`/`unordered`/`convertOperand` + `RelOptRuleOperand` ctor + `RelOptRuleOperandChildren` | **RESOLVED (owner decision: mirror Calcite; folds in A47/A48).** The earlier fused `Any<TOp>()`/`Leaf`/`Some`/`Unordered` factories (with a fabricated "Alembic-original DSL" rationale, retracted) were rebuilt to Calcite's **two layers**: ported `OpRuleOperandChildren` (policy + operand list, `AnyChildren`/`LeafChildren`); `Some`/`Unordered`/`None`/`Any` now return `OpRuleOperandChildren`; `Operand<TOp>(children)` / `OperandJ<TOp>(trait, predicate, children)` build the operand — so a rule reads `Operand<Foo>(Some(Operand<Bar>(Any())))` as Calcite reads `operand(Foo, some(operand(Bar, any())))`. `OpRuleOperand` was collapsed to Calcite's **single** ctor (transcribed: the `childPolicy` switch `checkArgument`/`assert` validation + the `child.parent`/"cannot re-use operands" wiring — A48 guards previously dropped), taking `ImmutableArray<OpRuleOperand>` (Calcite's `ImmutableList`); its four non-Calcite convenience ctors were deleted and every `new OpRuleOperand(...)` (ConvertOperand, TraitMatchingRule) routed through it. **Naming:** the static builder takes `Operand` (= Calcite `operand`); the instance accessor `getOperand()` became the method `GetOperand()` (= Calcite `getOperand()`) since a property and same-named static method cannot coexist in C#. *Update (2026-06-24):* the converter-operand fusion is now undone — `ConverterRelOptRuleOperand` was recreated as the nested `ConverterOpRuleOperand : OpRuleOperand` (transcribed; carries the converter-on-converter guard in its `Matches`), `OpRuleOperand` un-sealed with `Matches` virtual and the `IsConverterOperand` flag removed, and `ConvertOperand<TOp>(predicate, trait)` restored the dropped predicate param to mirror `convertOperand(Class, Predicate, RelTrait)`. *Still deferred:* `RelOptRuleOperand.toString()`/`describeIt()` unported. |

### 2b. Volcano

| # | Alembic member | Calcite member | Finding |
|---|---|---|---|
| ✅ A6 | `VolcanoPlanner.Register`/`RegisterImpl` | `register`/`registerImpl` | **RESOLVED (2026-06-24).** Translated the entry guards: `Register` and `RegisterImpl` got `Debug.Assert(!IsRegistered(op))` (Java `assert`); `RegisterImpl` got the planner-identity check (`if (!ReferenceEquals(op.Cluster.Planner, this)) throw …`) and the trait-count check (`if (op.Traits.Count != _traitDefs.Count) throw …`) — both translating Calcite's `throw new AssertionError` as `InvalidOperationException` (the idiom the adjacent convention-interface check already uses). The trait-count guard also caught two Cascades tests building ops with an incomplete trait set (1 trait vs 2 registered dims); fixed to carry the sortedness dimension like their siblings. |
| ✅ A7 | `VolcanoPlanner.PropagateCostImprovements` | `propagateCostImprovements` | **RESOLVED (2026-06-24).** Transcribed exactly: a `Util.PriorityQueue<IOp>` (the new `java.util.PriorityQueue` analog) ordered by a comparator that reads the cost map, with the decrease-key `Remove(parent)`-then-re-`Offer` on a cost drop; seeds/updates via `GetCostOrInfinite` (A39); and the `mq.ClearCache(subset)`/`mq.ClearCache(parent)` calls restored. **Prerequisites ported first:** (1) `Util.PriorityQueue` (`java.util.PriorityQueue` analog with `Remove`); (2) A39 (`GetCost`→`@Nullable` + `GetCostOrInfinite`). **Omitted (owner decision):** `subset.timestamp++` — its only consumer is the `@Deprecated` `CachingRelMetadataProvider`, superseded by the per-query cache + `ClearCache` (D12); a code comment records the omission. |
| ✅ A8 | `VolcanoPlanner.AddOpToSet` | `addRelToSet` | **RESOLVED (2026-06-24).** Wrapped the `PropagateCostImprovements(op)` call in `try { … } catch (CyclicMetadataException) { /* ignore */ }`, with Calcite's "give the subset another chance to figure out its cost" comment. (Verified the *other* `propagateCostImprovements` call site — the equivalence-collapse `equiv` one — is **bare** in Calcite too, so it correctly stays unwrapped.) |
| ✅ A9 | `VolcanoPlanner.ChangeTraits` | `changeTraits` | **RESOLVED (2026-06-24).** Restored the early-return (`if (subset.Traits.Equals(toTraits)) return subset;`) and both asserts (`!op.Traits.Equals(toTraits)`, `toTraits.AllSimple()`). The `!Equals` assert required first implementing A10 (below): a Cascades test had been calling `ChangeTraits(root, <root's own traits>)` — a contract-violating no-op that compensated for `SetRoot` not running `ensureRootConverters`; with A10 done, that redundant line was removed and the assert holds. |
| ✅ A10 | `VolcanoPlanner.SetRoot` | `setRoot` | **RESOLVED (2026-06-24).** `SetRoot` now calls `EnsureRootConverters()` eagerly after registering the root, transcribing Calcite's `setRoot` (Calcite calls it in both `setRoot` and `findBestExp`; Alembic now matches). `originalRoot` stays omitted (materialization/relational); `rootConvention` is the existing `RootConvention` property. |
| ✅ A11 | `OpSet.Add` | `RelSet.add` | **RESOLVED (2026-06-24).** Added `Debug.Assert(EquivalentSet is null, "adding to a dead set")` and computed the subset from `op.Traits.Simplify()` (was `op.Traits`), matching `rel.getTraitSet().simplify()`. |
| ✅ A12 | `OpSet.AddConverters` | `addConverters` | **RESOLVED (2026-06-24).** Translated the dropped `from.Traits.Convention is not null` guard term and the `toTrait is null \|\|` half of the per-trait break (declaring `IOpTrait? toTrait`). Both mirror Calcite verbatim; they're defensive-only today because `OpTraitSet.Convention`/`Get` are non-nullable in Alembic (a separate pre-existing divergence, left untouched). |
| ✅ A13 | `OpSet.AddInternal`/`GetOrCreateSubset`/`MergeWith` | `addInternal`/`getOrCreateSubset`/`postEquivalenceEvent`/`mergeWith` | **RESOLVED (2026-06-24).** `postEquivalenceEvent` was mislocated entirely: Alembic fired equivalence events from an invented planner method `AbstractOpPlanner.FireOpEquivalenceFound` (fabricated provenance to a non-existent `notifyEquivalence`). Translated to where Calcite has it — a private `OpSet.PostEquivalenceEvent` called from `AddInternal` and the new-subset branch of `GetOrCreateSubset` (`Subsets.Add` first, then the listener-guarded fire) — and the planner method deleted. The `mergeWith` asserts (entry `this != otherSet`/`equivalentSet == null` ×2, `existed` on removal, the two mid-method `equivalentSet == null`, and the per-op `getSet(rel) == this`) were all translated as `Debug.Assert`. Logging (`LOGGER.trace`) omitted; `!(otherRel instanceof Spool)` is relational. |
| ✅ A14 | `OpSubset.Add` | `RelSubset.add` | **RESOLVED (2026-06-24).** Translated `RelSubset.add`'s inline listener firing exactly — equivalence class = the subset (`this`), `physical = true` (the literal Calcite passes) — replacing the misplaced `FireOpEquivalenceFound(op, Set.Id, !Convention.Equals(None))` call. |
| ✅ A15 | `OpSubset.Explain` | `RelSubset.explain` | **RESOLVED (2026-06-24).** Alembic had overridden the *wrong* method — `ExplainTerms` (calling `base.ExplainTerms` + `Item("subset", Set.Id)`) — whereas Calcite's `RelSubset` overrides `explain(RelWriter)` and does the work directly. Made `AbstractOp.Explain` `virtual` (matching `AbstractRelNode.explain` being overridable), deleted the bad `ExplainTerms` override, and translated `explain` exactly: `pw.Item("subset", ToString()); var input = Util.First(Best, GetOriginal()); if (input is null) return; input.ExplainTerms(pw); pw.Done(input);`. Ported `org.apache.calcite.util.Util.first` as `Alembic.Util.Util.First<T>`. **Dependencies surfaced & ported:** (1) `RelSubset.deepEquals`/`deepHashCode` (reference identity — `this==obj`/`this.hashCode()`) were never ported; the old `Set.Id`-in-digest hack had been emulating them, so removing it required porting them faithfully (`ReferenceEquals`/`RuntimeHelpers.GetHashCode`). (2) `AbstractRelNode.toString()` (`"rel#"+id+":"+getDigest()`) ported to `AbstractOp.ToString()`; `RelSubset.getDigest()` (String, `"RelSubset#"+id+"."+traits`) ported to `OpSubset.ToString()`. The two Calcite digest accessors were then properly disentangled (2026-06-24): Alembic had mis-mapped both onto one object accessor named `GetDigest()`. Now `getRelDigest()` → `GetOpDigest()` (the `IOpDigest` object, used as the dedup-map key at all 13 sites in `VolcanoPlanner`/`HepPlanner`), `getDigest()` (String) → a new virtual `GetDigest() => GetOpDigest().ToString()`, `toString()` → `AbstractOp.ToString() = "rel#"+Id+":"+GetDigest()`, and `RelSubset.getDigest()` → `OpSubset.GetDigest()` override (`"RelSubset#"+Set.Id+"."+Traits`). `OpSubset` no longer overrides `ToString()` (Calcite's `RelSubset` doesn't either — it inherits `toString()` and overrides only String `getDigest()`). Every provenance is now exact. |
| ✅ A16 | `OpSubset.CheapestPlanReplacer.Visit` | `CheapestPlanReplacer.visit` | **RESOLVED (2026-06-24).** Now memoizes by `p.Id` (`Dictionary<int, IOp>`, `pId` captured first, `visited[pId] = p`) and detects change with the post-loop whole-list `!inputs.SequenceEqual(oldInputs)` — matching Calcite's `getId()` key + `!inputs.equals(oldInputs)`. Behavior was already equivalent (ids and refs are both 1:1 per op; `AbstractOp` has no `Equals`/`GetHashCode` override), so this is a structural alignment. The `provenanceMap.put(p, DirectProvenance(pOld))` update stays omitted: Calcite's `Provenance`/`provenanceMap` is an unported debug-trace subsystem (🔵, gated behind `LOGGER.debug`). |
| ✅ A17 | `OpSubset` ctor / `ComputeBestCost` / `StartOptimize` / `DisableEnforcing` | `computeBestCost` / `startOptimize` / `disableEnforcing` | **RESOLVED (2026-06-24).** `ComputeBestCost` null-cost guard restored (via A39); ctor `Debug.Assert(traits.AllSimple())` already present; added the two dropped entry asserts — `DisableEnforcing` → `Debug.Assert(IsDelivered)` (`assert isDelivered()`), `StartOptimize` → `Debug.Assert(GetWinnerCost() is null, this + " is already optimized")` (`assert getWinnerCost() == null : …`). |
| ✅ A18 | `DeferringRuleCall` ctor / `OnMatch` | `VolcanoPlanner.DeferringRuleCall` | **RESOLVED (2026-06-24).** `nodeInputs` now threads `OnMatch` → `VolcanoRuleMatch` → `VolcanoRuleCall` (new 4-arg `protected` ctor) → `OpRuleCall`, mirroring Calcite; exposed `OpRuleCall.NodeInputs` `protected` to match `RelOptRuleCall.nodeInputs`; the `allNotNull` check became `Debug.Assert`; and the three `DeferringRuleCall` provenances were corrected from `VolcanoRuleCall.DeferringRuleCall` to `VolcanoPlanner.DeferringRuleCall` (the actual enclosing class). The `rels.clone()` is subsumed by `ImmutableArray`'s immutability. (A drive-by `GetRels`/`GetParentRels`/`GetChildRels`→`…Ops` rename rode along — kept, as it aligns with the Op-naming convention.) |
| ✅ A19 | `TopDownRuleDriver.EnsureGroupExplored.Perform` | `EnsureGroupExplored.perform` | **RESOLVED (2026-06-24).** Added the post-`SetExplored()` loop `foreach (var subset in _input.Set.Subsets) _input.Cluster.GetMetadataQuery().ClearCache(subset);` (with Calcite's "Clear the LB cache as exploring state has changed" comment), translating the dropped LB-metadata-cache clear. |
| ✅ A20 | `IterativeRuleDriver.Drive` / `TopDownRuleDriver.Drive` | `drive` | **RESOLVED (2026-06-24).** Added `Debug.Assert(match.Rule.Matches(match));` after the pop/null-check in `IterativeRuleDriver.Drive` (the only in-scope item — Calcite's `TopDownRuleDriver.drive` has no such assert). `exploreMaterializationRoots()` seeding (MV) and the timeout-catch `LOGGER.warn` remain 🔵 (blacklisted / logging). |
| ✅ A21 | `RuleQueue.HasDuplicateSubsetOnPath` / `TopDownRuleQueue` | `checkDuplicateSubsets` / queue | **RESOLVED (2026-06-24).** Added the dropped post-pop assert: `var x = subsets.Pop(); Debug.Assert(ReferenceEquals(x, subset));`. The two other audit notes were false flags: Calcite's `matches` is `HashMap<RelNode,…>`, which is *identity*-keyed (RelNode doesn't override `equals`), so Alembic's `ReferenceEqualityComparer` is the faithful rendering (and the codebase's consistent idiom — cf. `propagateOps`, `initOpToVertexCache`, `changedRels`); and `matches` is a `private` field, which per convention correctly carries no `[Provenance]`. |
| ⚠️ A22 | `VolcanoCost.IsLessThanOrEqual` / `IsLessThan` | `VolcanoCost.isLe` / `isLt` | Compares `_cpu` where Calcite's live path compares `rowCount` (its `if(true)` ignores cpu). `rows` is relational/dropped, so cpu is the natural covered dimension — **confirm this is the intended substitution**. |
| ⚠️ A23 | `VolcanoRuleMatch` ctor | `VolcanoRuleMatch` | Null-check is an always-on `throw` (Calcite uses `assert allNotNull`); no `rels.clone()` defensive copy; `RecomputeDigest` lacks the `[Obsolete]`/deprecation marker. |
| 🔵 A24 | `ExpandConversionRule` / `VolcanoTimeoutException` | `AbstractConverter.ExpandConversionRule` / `VolcanoTimeoutException` | `ExpandConversionRule` realizes `INSTANCE`/operand via direct ctor since `RelRule.Config` is unported (D30); `VolcanoTimeoutException` is `sealed` vs Calcite non-final, drops the explicit null cause. |
| ✅ A25 | `AbstractConverter`, `IterativeRuleQueue`, `RuleQueue` core | — | Faithful (Multimap list-vs-set semantics, logging drops are benign). |

### 2c. HEP

| # | Alembic member | Calcite member | Finding |
|---|---|---|---|
| ✅ A26 | `HepPlanner.TryCleanVertices` / `ClearCache` | `tryCleanVertices` / `clearCache` | **RESOLVED (2026-06-24).** `ClearCache` was a *reimplementation*, not a translation: it recomputed digests up a `seen`-set walk, where Calcite clears the **metadata** cache up a walk gated on the cache-presence bool. Rewrote `ClearCache` to translate Calcite exactly (clearing via a provenance'd `ClearMetadataCache(IOp) => op.Cluster.GetMetadataQuery().ClearCache(op)` = `RelMdUtil.clearCache(RelNode)`), dropped the digest-recompute entirely (verified harmless — `_mapDigestToVertex` is keyed on the op digest and re-keyed by `UpdateVertex`, so the recompute served no purpose), and added the dropped `clearCache(vertex)` call + the two `Debug.Assert(…Edges.Count == 0)` in `TryCleanVertices`. |
| ✅ A27 | `HepPlanner` GC sets | `collectGarbage` / `tryCleanVertices` | **RESOLVED (2026-06-24).** `outVertices` and the `garbage` (garbageVertexSet) set are now `LinkedHashSet<HepOpVertex>`, matching Calcite (deterministic discard/clean order). `rootSet`/`sweepSet` correctly stay `HashSet` — Calcite uses plain `HashSet` for those. |
| ✅ A28 | `HepPlanner.ApplyTransformationResults` | `applyTransformationResults` | **RESOLVED (2026-06-24, via A39):** added `if (thisCost is null) continue;` before the cost compare. |
| ✅ A29 | `HepPlanner.BuildFinalPlan` / `ChangeTraits` | `buildFinalPlan` / `changeTraits` | **RESOLVED (2026-06-24).** `BuildFinalPlan` rewritten to translate Calcite's in-place algorithm exactly — `op.ReplaceInput(i, BuildFinalPlan(child))` (ops *are* mutable: `BiOp`/`SingleOp` override `ReplaceInput`, already used in `ContractVertices`), the `changed` → `ClearMetadataCache`/`RecomputeDigest`, and the `if (op is HepOpVertex) throw` post-condition; the functional Copy + memo dict (a mistranslation, not a forced adaptation) are gone — shared vertices are handled by the in-place "already replaced" skip, like Calcite. Removed the non-Calcite `_rootOp` field: `ChangeTraits` now compares against `_root` (the vertex, = Calcite's `rel == root`) and `_root.CurrentOp` (= `root.getCurrentRel()`). |
| 🔵 A30 | `HepPlanner` `onCopyHook` / `isRegistered` | `onCopy`/`onCopyHook`/`isRegistered` | The copy-hook callback and `isRegistered` are absent (callback plumbing unported). |
| ✅ A31 | `HepInstruction.*` ctors | instruction ctors | **RESOLVED (2026-06-24).** The three `RuleClass`/`ConverterRules`/`CommonRelSubExprRules` `State.RuleSet` changed from `List<OpRule>` to `LinkedHashSet<OpRule>` — Calcite's `new LinkedHashSet<>()` (insertion-ordered *and* deduping); `EndGroup.State.RuleSet` correctly stays `HashSet` (Calcite uses `new HashSet<>()` there). The dropped `requireNonNull` guards are idiom-subsumed (non-null ctor params, per the A33 precedent), and the nested-`State` `Instruction` fields are the correct forced-idiom rendering of Java's enclosing-instance capture (same as `HepProgram.State._program`, A32) — no provenance is correct for those. |
| ✅ A32 | `HepProgram.State` ctor | `HepProgram.State` | **RESOLVED (2026-06-24).** Calcite's `State.execute()` uses `HepProgram.this` (Java enclosing-instance), so the `_program` field is the *correct* forced-idiom rendering (C# nested classes don't capture the outer instance) and rightly has no provenance — now commented as such. The bug was folding that with Calcite's actual `List<HepInstruction>` ctor param: the ctor now takes both `(px, HepProgram program, IReadOnlyList<HepInstruction> instructions)` and iterates `instructions` (caller passes `this, Instructions`), making the `State(…, List<HepInstruction>)` provenance honest. |
| ✅ A33 | `HepOpVertex` ctor / `Copy` / `Explain` / `GetDigest` | `HepRelVertex` | **RESOLVED (2026-06-24).** Ctor now has `if (currentOp is HepOpVertex) throw new ArgumentException();` (`checkArgument`; the `requireNonNull` is subsumed by the non-null `IOp` type + base deref). `Copy` translates both asserts (`Debug.Assert(traits.Equals(Traits))` / `Debug.Assert(children.SequenceEqual(Children))`). The wrong `ExplainTerms` override was deleted in favour of `Explain(IOpWriter) => _currentOp.Explain(pw)` (Calcite overrides `explain(RelWriter)`, the A15 pattern), and a String `GetDigest() => "HepRelVertex(" + _currentOp + ")"` override was added to keep the display digest faithful (dedup is unaffected — keyed via `DeepEquals`/`DeepHashCode` on `_currentOp`). |
| ✅ A34 | `HepVertexIterator` | `HepVertexIterator` | **RESOLVED (2026-06-24).** Visited-set changed from `HashSet<HepOpVertex>` to `HashSet<int>` keyed by `target.Id` (= Calcite's `Set<Integer>` of `getId()`) — which also makes the ctor's existing `Set<Integer>` provenance accurate; updated the two `_visited.Add` sites and the two `HepPlanner` call-site `new HashSet<int>()`. Removed the `Reset()`→`remove()` provenance (analogous-not-exact: `Reset` is a C# `IEnumerator` requirement, like the un-provenanced `Dispose`; Calcite's throwing `remove()` stub has no C# counterpart). `Of` re-enumeration was a non-issue — the `yield` method builds a fresh iterator per enumeration, matching Calcite's `() -> new HepVertexIterator(...)`. |
| 🔵 A35 | `HepRuleCall.TransformTo` | `transformTo` | Drops `RelOptUtil.verifyTypeEquivalence` (row-type check — relational) and `handler.propagate` (hints); the `InvalidateMetadataQuery()` ordering is faithful. |
| ✅ A36 | `HepProgramBuilder` | `HepProgramBuilder` | **RESOLVED (2026-06-24).** The `Check` helper was removed in A75 (each `checkArgument` inlined as `if (!c) throw new ArgumentException();`). Now also added the explicit `public HepProgramBuilder()` ctor and the factored `Clear()` method (`_instructions.Clear(); _group = -1;`), called from `Build()` — matching Calcite's `clear()` ("clearing the builder state as a side-effect of build"). |
| ✅ A37 | `HepState` (base) | `HepState` | Faithful (planner/programState fields, abstract `Execute`, empty virtual `Init`). |

### 2d. Plan core

| # | Alembic member | Calcite member | Finding |
|---|---|---|---|
| ✅ A38 | `AbstractOpPlanner.OnNewClass` | `AbstractRelOptPlanner.onNewClass` | **RESOLVED (2026-06-24).** Ported the missing `IOp.Register(IOpPlanner)` hook (= `RelNode.register`; `AbstractOp.Register` is a no-op, matching `AbstractRelNode.register`'s `Util.discard(planner)`) and made the base `OnNewClass` call `op.Register(this)` like Calcite. Also added the dropped `base.OnNewClass(op)` to `VolcanoPlanner.OnNewClass` (Calcite's override does `super.onNewClass(node)` first). Behaviour-equivalent today (no op overrides `Register`), but the protocol hook is now faithful. |
| ✅ A39 | `AbstractOpPlanner.GetCost` / `VolcanoPlanner.GetCost` | `getCost(rel, mq)` | **RESOLVED (2026-06-24).** Both `GetCost` overloads now return `IOpCost?` (dropped the `!`), with `VolcanoPlanner.GetCost` transcribing Calcite's null-returns (`GetNonCumulativeCost` null → null; per-input `null` → null). Added `VolcanoPlanner.GetCostOrInfinite` (reusing the existing `InfiniteCost`/`infCost` member). This also restored the dropped null-cost guards: A17 (`OpSubset.ComputeBestCost`) and A28 (`HepPlanner.ApplyTransformationResults`). |
| ✅ A40 | `AbstractOpPlanner.AddRule` | `addRule` | **RESOLVED (2026-06-24).** The exception type was already faithful: Calcite uses `throw new AssertionError(...)` — an *unconditional* throw, not an `assert` statement — which maps to `throw new InvalidOperationException`, **not** `Debug.Assert` (that would be conditional and skip in release). The audit's "should be Debug.Assert" misread the construct. Fixed the real divergence: the message now matches Calcite verbatim (`"Rule's description should be unique; existing rule=" + existing + "; new rule=" + rule`, using the rules' `ToString()` = `Description`). |
| ⚠️ A41 | `AbstractOpPlanner` `fireRule` orchestration / `AddListener` | `fireRule` / `addListener` | Calcite's `fireRule` (checkCancel + `assert matches` + exclusion checks + `onMatch` + before/after events) is distributed into `VolcanoRuleCall.OnMatch`/`HepPlanner.ApplyRule` + a thin `FireRuleAttempted`; `AddListener` promotes to multicast on the 2nd add vs Calcite's always-wrap. (Behaviour-preserving relocation — confirm.) |
| 🔵 A42 | `AbstractOpPlanner` rule-attempts tracking | `RuleAttemptsListener`/`dumpRuleAttemptsInfo`/`RuleEventLogger` | The non-relational rule-attempts tracing/benchmark subsystem and its ctor wiring are omitted. |
| ⚠️ A43 | `OpTraitSet.Contains`/`Comprises`/`Matches`/`Difference` | same | Compare with `Equals` (value) where Calcite uses reference `==` on canonized traits; inconsistent with `Equals`/`EqualsSansConvention` which do use `ReferenceEquals` (traits are interned, so equivalent — flagged for consistency). |
| ⚠️ A44 | `OpTraitSet.Get(OpTraitDef)` / `Replace<T>(def,trait)` / `Get(int)` | `getTrait`/`replace(int,…)` | `Get(OpTraitDef)` returns `def.Default` when absent (Calcite returns `null`); `Replace<T>(def,trait)` is provenanced to `replace(int,…)` but is a def-keyed overload that skips the canonize/identity fast-path and assert; `Get(int)` error text + missing `getTraits(int)` overload. |
| ⚠️ A45 | `OpCompositeTrait` ctor | `RelCompositeTrait` ctor | Drops both asserts (strictly-ordered members; per-member `traitDef` match); adds a non-generic base class with no Calcite counterpart; `Equals` drops the identity fast-path. |
| ⚠️ A46 | `ConventionTraitDef.Convert` | `convert` | Recomputes the metadata query from the evolving `converted` op inside the arc loop rather than once from `rel`; drops the path-endpoint and `changeConvention` asserts and the `deregisterConverterRule` `assert removed`. |
| ⚠️ A47 | `OpRule` ctor / `ConvertOperand` / `AssignSolveOrder` | `RelOptRule` | Ctor drops the operand null-guard; `ConvertOperand<TOp>(IOpTrait)` is provenanced to the 3-arg predicate overload but drops the predicate; `AssignSolveOrder` drops `assert m==operands.size()`; `getOutConvention`/`getOutTrait`/`guessDescription`/`equals(RelOptRule)` absent. |
| ⚠️ A48 | `OpRuleOperand` ctor / convenience ctors | `RelOptRuleOperand` | Ctor drops the `childPolicy` switch validation (`checkArgument`/`assert`) and the parent-wiring loop (relocated to flattening); `toString()`/`describeIt` not ported; adds four no-provenance `internal` ctors + `AlwaysMatches` (non-Calcite convenience members). The inline `IsConverterOperand` n²-guard is D27's intentional fusion. |
| ⚠️ A49 | `OpRuleCall.TransformTo` / ctor | `RelOptRuleCall` | The two-arg `TransformTo` is `abstract` (Calcite's two-arg is a concrete delegate to the three-arg `+RelHintsPropagator` form); ctor drops `assert rels.length==rule.operands.size()`. (`builder()`/`getRels()`/`LOGGER`/hint overloads relational/🔵.) |
| ✅ A50 | `OpTraitDef.Canonize` / `CanConvert` / `Convert` | `RelTraitDef` | **RESOLVED (2026-06-24).** `Canonize` regained the `Debug.Assert(trait is OpCompositeTrait || TraitType.IsInstanceOfType(trait))` guard; `CanConvert`/`Convert` are now **`abstract`** and **strongly typed** — the generic `OpTraitDef<TTrait>` declares `Canonize(TTrait):TTrait` / `CanConvert(…,TTrait,TTrait)` / `Convert(…,TTrait,…)` (the faithful `RelTraitDef<T>` signatures), and the non-generic base keeps the erased `IOpTrait` entry points as **sealed bridge overrides** that downcast — the C# analog of javac's synthesized bridge methods. Concrete defs (`ConventionTraitDef`, `SortKey`/`SortednessTraitDef`) now override the typed forms with no internal cast; `OpCompositeTrait.Of` dropped its `(T)Canonize` cast. (Generic-split + concrete `TraitType` remain the known kept follow-up; the absent protected ctor is the only residual.) |
| ⚠️ A51 | `OpCluster.TraitSetOf` | `traitSetOf(RelTrait…)` | Builds via `result.Plus(trait)` where Calcite uses `traitSet.replace(trait)` (replace-at-dimension vs possibly-add); single-arg `traitSetOf` overload absent. |
| ✅ A52 | `PlanUtil.ToString` | `RelOptUtil.toString(RelNode)` | **RESOLVED (2026-06-24).** `ToString(IOp? op)` now returns `string?` with `if (op is null) return null;`. Threaded `withIdPrefix` through `OpWriterImpl` (new `OpWriterImpl(builder, withIdPrefix)` ctor; the `id:` prefix is now gated on it; the default `OpWriterImpl(builder)` keeps `true` like `RelWriterImpl(PrintWriter)`), and `ToString` builds the writer with `withIdPrefix: false` — matching Calcite's `new RelWriterImpl(pw, detailLevel, false)`. (`SqlExplainLevel` detail-level verbosity is the one piece still not threaded — Alembic always renders at the attribute level; noted on the ctor.) |
| ✅ A53 | `Contexts.ChainContext` ctor / `Chain` | `Contexts.ChainContext` / `chain` | **RESOLVED (2026-06-24).** Added the `foreach (var context in contexts) Debug.Assert(context is not ChainContext, "must be flat")` loop, and `Chain(params)` now does the defensive copy (`contexts.ToImmutableArray()`, = `ImmutableList.copyOf`). The `requireNonNull(contexts)` is subsumed — `Contexts` is an `ImmutableArray<IContext>` struct (non-null), per the A33 precedent. |
| 🔵 A54 | `MulticastPlannerListener` | `MulticastRelOptListener` | `OpDiscarded`/`OpChosen` declared in swapped order vs Calcite `relChosen`/`relDiscarded`; list initialized inline rather than in the ctor. Cosmetic. |
| ✅ A55 | `Convention`, `OpCost` | `Convention.Impl`, `RelOptCostImpl` | Faithful (reference-identity `Satisfies`, NONE singleton, no by-name equals; cost arithmetic). Only cosmetic member-ordering and the inlined `Epsilon`/`makeCost` cpu-vs-dRows (relational) notes. |

### 2e. Algebra

| # | Alembic member | Calcite member | Finding |
|---|---|---|---|
| ⚠️ A56 | `AbstractOp.Copy` (abstract) | `AbstractRelNode.copy` | Calcite's base `copy` is concrete (returns `this` when traits+inputs unchanged, else `AssertionError`); Alembic makes it `abstract`, dropping that default + post-condition. |
| ⚠️ A57 | `AbstractOp.OnRegister` | `onRegister` | Drops the input-change `assert`/`isValid` guards and restructures change-detection (per-element `!ReferenceEquals` vs Calcite's `Util.equalShallow`). |
| 🔵 A58 | `AbstractOp.DeepHashCode`/`DeepEquals`/digest naming | `deepHashCode`/`deepEquals`/`getRelTypeName` | Omits the `Hintable` `hints` digest item (hints relational); `getRelTypeName` inlined as `GetType().Name`; `toString`/`getDigest()` string/`sole`/`getInput`/`accept(RelShuttle)` not ported (debug/shuttle). |
| ✅ A59 | `SingleOp`/`BiOp` `ChildrenAccept` | `SingleRel`/`BiRel.childrenAccept` | **Over-flag in raw reports.** Calcite's per-class `childrenAccept` is subsumed by Alembic's generic `AbstractOp.ChildrenAccept` (iterates `Children`, visits in order) — visitor descent is correct; only the `[Provenance]` cites the empty base body. No override needed. |
| ⚠️ A60 | `IOp.ComputeSelfCost` | `RelNode.computeSelfCost` | Non-nullable `IOpCost` return vs Calcite's `@Nullable`; `getDescription`/`register`/`isValid`/`getRelTypeName`/`accept(RelShuttle)` absent (debug/structural). |
| ⚠️ A61 | `OpWriterImpl.Explain`/`Done` | `RelWriterImpl.explain_`/`done` | Drops the `mq.isVisibleInExplain` check + in-place child fallback, and the `checkInputsPresentInExplain` assert + `flush()`; uses one shared builder + `\n` instead of a per-line `StringBuilder`/`println`; `simple()` not ported. |
| ✅ A62 | `ConverterImpl` / `ConverterRule` / `TraitMatchingRule` | converter family | **`ConverterRule` resolved (2026-06-24):** restored the ctor's `Debug.Assert(source.TraitDef == target.TraitDef)`, the `out` shortcut field (`Out = target as IConvention`), and `getOutConvention()` (`(IConvention)Target`) — plus recreated the base `OpRule.GetOutConvention()` (= `RelOptRule.getOutConvention()`, returns null) it overrides. **`TraitMatchingRule` resolved (2026-06-24):** restored `Debug.Assert(converterOperand.ChildPolicy == Any)` in `BuildOperand` and the `OutConvention` override (`_converterRule.OutConvention`). **`ConverterImpl.computeSelfCost` resolved (2026-06-24, owner decision):** reproduce Calcite's math (`dCpu = dRows; dIo = 0; makeCost(dRows, dCpu, dIo)`) with the relational row count elided — `dRows` is unported, so `dCpu`/`dIo` reduce to 0 and the cost comes out zero. |
| ✅ A63 | `OpVisitor` | `RelVisitor` | Faithful 1:1 (`Go`/`Visit`/`ReplaceRoot`). |

### 2f. Util & Metadata

| # | Alembic member | Calcite member | Finding |
|---|---|---|---|
| ⚠️ A64 | `DefaultDirectedGraph` vertex storage | `DefaultDirectedGraph` | Splits Calcite's single insertion-ordered `LinkedHashMap` into a `Dictionary` + parallel `_order` list + `VertexSetView` (added structure/bookkeeping); `RemoveEdge` drops the `inRemoved`/`outRemoved` assert; `EdgeSet`/`VertexSet` expose live/read-only views vs Calcite's unmodifiable/mutable; `source(E)`/`target(E)`/`toString` helpers absent. |
| ⚠️ A65 | `Graphs.MakeImmutable` / `PredecessorListOf` / `FindPathsExcluding` | `Graphs.FrozenGraph` | `MakeImmutable` seeds distances by iterating `EdgeSet` vs Calcite's per-vertex `outEdges` (order); `PredecessorListOf` returns an eager copy vs Calcite's live `AbstractList` view; `FindPathsExcluding` iterates `EdgeSet` vs the raw `edges` field; `FrozenGraph` is `sealed`. (`GetPaths` stable sort is faithful — D31.) |
| 🔴/⚠️ A66 | `TopologicalOrderIterator` | `TopologicalOrderIterator` | Ctor drops `assert order==TOP_DOWN||BOTTOM_UP`; `FindCycles` returns a snapshot `HashSet` vs Calcite's live `keySet()` view; `empties`/`countMap` order depends on `Dictionary` enumeration (both Java/.NET unspecified, but divergent); missing-key `continue` replaces Calcite's `requireNonNull`. |
| ⚠️ A67 | `DefaultEdge` ctor | `DefaultEdge` | Drops `requireNonNull` on both endpoints (Calcite NPEs on null; Alembic does not). |
| 🔵 A68 | `BreadthFirstIterator`/`DepthFirstIterator` `Of` / `Pair.Equals` | `of` / `Pair.equals` | `Of` reuses a single iterator instance (re-enumeration differs); `DepthFirstIterator.of` returns `buildList` directly in Calcite. `Pair.Equals` matches only `Pair<T1,T2>` vs Calcite's any-`Map.Entry` (Alembic has no `Map.Entry`, so moot). |
| ⚠️ A69 | `OpMetadataQueryBase.Map` | `RelMetadataQueryBase.map` | `protected readonly` vs Calcite's `public final` (Janino handlers reference it directly; Alembic's reflective `Cache` is internal, so defensible). |
| ⚠️ A70 | `ProxyingMetadataHandlerProvider.Resolve` | `ReflectiveRelMetadataProvider.Space.find` | Walks the full superclass chain before consulting interfaces, whereas Calcite checks each level's interfaces before ascending — most-specific-handler selection can differ when an interface handler sits on a mid-chain supertype. |
| 🔵 A71 | `ChainedOpMetadataProvider` ctor | `ChainedRelMetadataProvider` | Drops `assert !providers.contains(this)`; no `equals`/`hashCode` over `providers`. |
| 🔵 A72 | `OpMdCost` cumulative-cost overloads | `RelMdPercentageOriginalRows` | The `EnumerableInterpreter`/`Combine`-specific `getCumulativeCost` overrides are absent (adapter/non-core op types). |
| ✅ A73 | `OpMetadataQuery`(+Base), `OpMdLowerBoundCost`, `OpMdCost` (cumulative/non-cumulative), `OpMdMemory`, `OpMdParallelism`, `ReflectiveOpMetadataProvider`, `DefaultOpMetadataProvider` | — | Faithful for all in-scope members: per-query cache (ACTIVE→`CyclicMetadataException`, `NullSentinel`, op-row clear), cost/memory/parallelism handlers, and the reflective dispatch semantics (Janino path correctly not ported). |

_Audit coverage:_ every substantive Calcite-derived class went through a fresh cold-fork pass. Not separately
re-audited (settled as trivial/marker in prior passes): the marker interfaces (`IConverter`, `IContext`,
`ICommonSubExprRule`, `IOpImplementor`, `IPhysicalOp`, `IOpDigest`, `IMetadata*`, `IRuleDriver`,
`IWrapper`), the enums (`DeriveMode`, `HepMatchOrder`, `RuleOperandChildPolicy`), `CannotPlanException`, and
the non-Calcite Guava/infra ports (`Interner`/`Interners`, `Multimap`, `LinkedHashSet`, `ProvenanceAttribute`).

_Confirmed faithful this pass:_ `AbstractConverter`, `OpVisitor`, `Convention`, `OpCost` arithmetic, `HepState`,
`OpMetadataQuery` core + the cost/memory/parallelism Md handlers + lower-bound cost, `ReflectiveOpMetadataProvider`,
`DefaultOpMetadataProvider`, and the `IterativeRuleQueue`/`RuleQueue` cores.

**Triage note.** The cold agents flag every dropped Java `assert`/`requireNonNull` as a divergence (correct per
the scope rule — the faithful form is `Debug.Assert`/an argument guard). These dominate the ⚠️ count and are
low-risk individually (asserts compile out of release builds); the follow-up `D23` "port the remaining guards as
`Debug.Assert`" remains the umbrella for them. The 🔴 set (A1–A5) and the ordering/identity ⚠️s (A3/A7/A16/A21/A27/
A34/A66/A70) are where behaviour could actually drift and are worth a code look first.

### 2g. Spotted during review (2026-06-24) — all resolved

| # | Alembic | Calcite | Finding & resolution |
|---|---------|---------|----------------------|
| ✅ A74 | `HepPlanner.RemoveFiredRules` | `tryCleanVertices` / `collectGarbage(Set)` fired-cache cleanup | **RESOLVED.** `RemoveFiredRules` was a non-Calcite helper (no `[Provenance]`) DRY-extracting Calcite's two *different* inlined cleanup blocks — and it introduced a behavioural bug: it ran `firedRulesCacheIndex.removeAll` at the `tryCleanVertices` site, where Calcite clears only `firedRulesCache` (the index `removeAll` happens **only** in `collectGarbage`). Inlined both blocks faithfully at their sites and deleted the helper. |
| ✅ A75 | `HepProgramBuilder.Check` | `Preconditions.checkArgument` calls | **RESOLVED.** `Check` was a non-Calcite helper throwing `InvalidOperationException` with an invented message, where Calcite calls Guava `checkArgument` (→ `IllegalArgumentException`). Inlined each of the 8 sites as `if (!cond) throw new ArgumentException();` — matching Calcite and the existing `OpRuleOperand` idiom — and deleted the helper. |
| ✅ A76 | `OpRuleCall.Rels`/`Op`/`GetRelList`, `VolcanoRuleCall` | `RelOptRuleCall.rels`/`rel(int)`/`getRelList()` | **RESOLVED.** Calcite's single `public final RelNode[] rels` had been split into an immutable `OpRuleCall.Ops` (provenance `getRelList()`) **plus** a duplicate mutable `VolcanoRuleCall.Rels` (fabricated provenance `VolcanoRuleCall.rels`). Unified into one `public readonly IOp?[] Rels` on `OpRuleCall` (provenance `RelOptRuleCall.rels`), with `Op(int)`=`rel(int)` and a new `GetRelList()`=`getRelList()`; removed the duplicate field and threaded the mutable array through `VolcanoRuleCall`/`VolcanoRuleMatch`(clones)/`DeferringRuleCall`/`HepRuleCall`, exactly as Calcite does. |

---

## 3. Follow-ups (tracked, not yet done)

- **Re-evaluate the op mutability model.** Ops were immutable early in the port; this was abandoned (2026-06-23) to match Calcite's `RelNode`, which mutates in place during planning (`replaceInput` + recomputable digest) — `fixUpInputs`/`rename`/the converter recheck are now strict mutate-in-place ports, with `replaceInput` throwing on `AbstractOp` and mutating on `SingleOp`/`BiOp` (and any multi-input op such as `PhysicalFma`), as Calcite does. Once the port is complete, decide whether to restore immutability or keep Calcite's mutable model.
- **`OpTraitDef.TraitType` (was `TraitClass`) is concrete; Calcite's `getTraitClass()` is `abstract`.** Ours returns `typeof(TTrait)` from `OpTraitDef<TTrait>`; Calcite leaves it abstract. Minor divergence — **acceptable, kept**.
- **`OpTraitDef<TTrait>` generic-typing — tightened (2026-06-24, see A50).** Calcite types `canonize`/`canConvert`/`convert`/`getDefault` as `T`. Alembic now matches this on the generic `OpTraitDef<TTrait>` (the strongly-typed signatures concrete defs override), while the non-generic base retains the erased `IOpTrait` entry points as **sealed bridge overrides** that downcast — the stand-in for Java raw-type access (`trait.TraitDef.Canonize(trait)`) and a faithful mirror of javac's bridge methods. *Residual:* the non-generic base still **declares** the erased members (Java has one class), and `OpTraitDef<IOpTrait>` would collide the bridge with the typed overload — a latent edge case, same as Java's erasure, not instantiated in practice.
- **Port the remaining Java `assert`/`checkArgument` guards as `Debug.Assert`/argument guards** (umbrella for the bulk of the ⚠️ findings in §2 — was `D23`). Most-affected: `VolcanoPlanner.RegisterImpl`, `OpSet`/`OpSubset`, the Hep ctors/`State`, `OpRuleOperand`/`OpRule` ctors, `Contexts.ChainContext`, the graph iterators, `DefaultEdge`.
